; PlatformIO Project Configuration
; HuginnESP — Multi-board ESP32 scanner firmware
;
; Supported boards:
;   esp32s3box  — Waveshare ESP32-S3-Touch-LCD-4B (N16R8, 4" 480x480 RGB display)
;   esp32c5     — Waveshare ESP32-C5-WIFI6-KIT (16MB flash / 8MB PSRAM, dual-band Wi-Fi 6, no display)
;   esp32c5zero — Waveshare ESP32-C5-Zero / Zero-M (4MB flash, no PSRAM, dual-band Wi-Fi 6, no display)
;   esp32       — Generic ESP32 DevKit (classic dual-core LX6, 4MB flash, headless)
;
; Build a specific board:  pio run -e esp32s3box   |   pio run -e esp32c5   |   pio run -e esp32c5zero   |   pio run -e esp32
; Upload a specific board: pio run -e esp32c5zero -t upload

[platformio]
default_envs = esp32s3box

[env]
framework     = arduino
monitor_speed = 460800
upload_speed  = 921600

; ---------------------------------------------------------------------
; Waveshare ESP32-S3-Touch-LCD-4B  (with 480x480 RGB touch display)
; ---------------------------------------------------------------------
[env:esp32s3box]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
board    = esp32-s3-devkitc-1

board_build.mcu                  = esp32s3
board_build.f_cpu                = 240000000L
board_build.flash_size           = 8MB
board_build.arduino.memory_type  = qio_opi
board_build.partitions           = default_8MB.csv

build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DCORE_DEBUG_LEVEL=3
    -DBOARD_HAS_PSRAM
    -DCONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP=1
    -DHUGINN_BOARD_S3=1
    -DHUGINN_HAS_DISPLAY=1

lib_deps =
    moononournation/GFX Library for Arduino@^1.6.0

; ---------------------------------------------------------------------
; Waveshare ESP32-C5-WIFI6-KIT  (dual-band Wi-Fi 6, BLE 5, no display)
; ESP32-C5-WROOM-1 N16R4 (16MB flash, 4MB PSRAM), single-core RISC-V @240MHz
;
; Uses pioarduino 55.03.38-1 (Arduino 3.3.8 / ESP-IDF 5.5.4) — first wave
; of pioarduino releases with esp32-c5 board definitions. Kept separate
; from the S3 env so the working S3 build isn't disturbed.
;
; ARDUINO_USB_MODE=1 selects USB-Serial-JTAG (HWCDC) — required on C5,
; which lacks the native USB OTG controller used by S2/S3. Without it
; the framework expands `Serial` to `USBSerial`, which doesn't exist
; on this chip and breaks the BLE library compile.
; ---------------------------------------------------------------------
[env:esp32c5]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board    = esp32-c5-devkitc1-n16r4

board_build.f_cpu      = 240000000L
board_build.partitions = default_16MB.csv

build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DCORE_DEBUG_LEVEL=3
    -DBOARD_HAS_PSRAM
    -DHUGINN_BOARD_C5=1
    -DHUGINN_HAS_DISPLAY=0
    -DHUGINN_HAS_SKIMMER_LED=1
    -DHUGINN_HAS_MODE_BUTTON=1

; ---------------------------------------------------------------------
; Waveshare ESP32-C5-Zero / Zero-M
; ESP32-C5-HF4 (4MB flash, no PSRAM), single-core RISC-V @240MHz
;
; Uses the generic ESP32-C5 DevKitC-1 4MB/no-PSRAM board definition because
; the Zero exposes the same ESP32-C5 class USB-Serial-JTAG interface and has
; the same 4MB flash constraint. The onboard WS2812 RGB LED is GPIO 29.
; ---------------------------------------------------------------------
[env:esp32c5zero]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board    = esp32-c5-devkitc-1

board_build.f_cpu      = 240000000L
board_build.flash_size = 4MB
board_build.partitions = partitions/huge_app_4MB.csv

build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DCORE_DEBUG_LEVEL=3
    -DHUGINN_BOARD_C5=1
    -DHUGINN_BOARD_C5_ZERO=1
    -DHUGINN_HAS_DISPLAY=0
    -DHUGINN_HAS_SKIMMER_LED=1
    -DHUGINN_LED_USE_NEOPIXELWRITE=1
    -DHUGINN_LED_BOOT_TEST=1
    -DSKIMMER_LED_PIN=29
    -DHUGINN_HAS_MODE_BUTTON=1

; ---------------------------------------------------------------------
; GPS-enabled variants — same as above but with -DHUGINN_HAS_GPS=1.
; Connect a GT-U7 / NEO-6M (or any NMEA module) to the pins below.
; Override GPS_RX_PIN / GPS_TX_PIN / GPS_UART_NUM here if needed.
; TX is declared but most receive-only breakout boards leave it NC.
; ---------------------------------------------------------------------
[env:esp32s3box-gps]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
board    = esp32-s3-devkitc-1

board_build.mcu                  = esp32s3
board_build.f_cpu                = 240000000L
board_build.flash_size           = 8MB
board_build.arduino.memory_type  = qio_opi
board_build.partitions           = default_8MB.csv

build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DCORE_DEBUG_LEVEL=3
    -DBOARD_HAS_PSRAM
    -DCONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP=1
    -DHUGINN_BOARD_S3=1
    -DHUGINN_HAS_DISPLAY=1
    -DHUGINN_HAS_GPS=1
    -DGPS_RX_PIN=17
    -DGPS_TX_PIN=18
    -DGPS_UART_NUM=1

lib_deps =
    moononournation/GFX Library for Arduino@^1.6.0

[env:esp32c5-gps]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board    = esp32-c5-devkitc1-n16r4

board_build.f_cpu      = 240000000L
board_build.partitions = default_16MB.csv

build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DCORE_DEBUG_LEVEL=3
    -DBOARD_HAS_PSRAM
    -DHUGINN_BOARD_C5=1
    -DHUGINN_HAS_DISPLAY=0
    -DHUGINN_HAS_SKIMMER_LED=1
    -DHUGINN_HAS_MODE_BUTTON=1
    -DHUGINN_HAS_GPS=1
    -DGPS_RX_PIN=17
    -DGPS_TX_PIN=18
    -DGPS_UART_NUM=1

[env:esp32c5zero-gps]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board    = esp32-c5-devkitc-1

board_build.f_cpu      = 240000000L
board_build.flash_size = 4MB
board_build.partitions = partitions/huge_app_4MB.csv

build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DCORE_DEBUG_LEVEL=3
    -DHUGINN_BOARD_C5=1
    -DHUGINN_BOARD_C5_ZERO=1
    -DHUGINN_HAS_DISPLAY=0
    -DHUGINN_HAS_SKIMMER_LED=1
    -DHUGINN_LED_USE_NEOPIXELWRITE=1
    -DHUGINN_LED_BOOT_TEST=1
    -DSKIMMER_LED_PIN=29
    -DHUGINN_HAS_MODE_BUTTON=1
    -DHUGINN_HAS_GPS=1
    -DGPS_RX_PIN=17
    -DGPS_TX_PIN=18
    -DGPS_UART_NUM=1

; ---------------------------------------------------------------------
; Generic ESP32 (classic dual-core LX6) — headless, 2.4 GHz WiFi + BLE.
; Works on any ESP32-DevKitC / NodeMCU-32S / Awok v3 / etc. with ≥4 MB
; flash and a CP2102 or CH340 USB-serial chip.
; No display, no PSRAM assumed. Override pins as needed.
; ---------------------------------------------------------------------
[env:esp32]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
board    = esp32dev

board_build.f_cpu      = 240000000L
board_build.partitions = default.csv

build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DHUGINN_BOARD_GENERIC=1
    -DHUGINN_HAS_DISPLAY=0

[env:esp32-gps]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
board    = esp32dev

board_build.f_cpu      = 240000000L
board_build.partitions = default.csv

build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DHUGINN_BOARD_GENERIC=1
    -DHUGINN_HAS_DISPLAY=0
    -DHUGINN_HAS_GPS=1
    -DGPS_RX_PIN=16
    -DGPS_TX_PIN=17
    -DGPS_UART_NUM=2
